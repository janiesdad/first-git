# First Git Example

This repository contains a simple React component for generating executive business intelligence prompts. The component is located in `src/StreamlinedPromptGenerator.jsx`.

The component includes configurable options for target company name and executive summary length, with a polished user interface designed for executive audiences.

To integrate this component into a React application, import the file and render `StreamlinedPromptGenerator` within your project.
